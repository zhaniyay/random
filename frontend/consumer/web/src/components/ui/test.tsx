// Test file to check if components compile
import { Button, Card } from './index';

export const TestComponent = () => {
  return (
    <div>
      <Card>
        <Button>Test Button</Button>
      </Card>
    </div>
  );
};
